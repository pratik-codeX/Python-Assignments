# Python Assignments
This repository contains solutions to foundational Python questions focusing on variables, data types, and memory.
It explores the usage of built-in functions like `type()` and `id()` to inspect memory addresses.
Key concepts covered include the differences between mutable objects (like lists) and immutable primitives.
These programs provide practical demonstrations of Python's internal memory allocation and object referencing.
